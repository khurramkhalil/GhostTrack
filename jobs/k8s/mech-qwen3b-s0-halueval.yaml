# GhostTrack — Qwen2.5-3B-Instruct | Seed 0 | HaluEval Track + Detect
#
# Reuses SAE checkpoints already on PVC from the triviaqa run.
# Runs track → detect on HaluEval QA pairs (clean paired factual/hallucinated).
#
# Why HaluEval for instruct models: TriviaQA AUROC was 0.559 because:
#   (a) chat_template prompt tokens dominate the sequence (~35 tokens) while
#       the answer is only 2-4 tokens — tracking signal is diluted
#   (b) LLM judge rejects 57% of string-match positives, leaving 18% factual
#   HaluEval provides full-sentence paired examples of equal length — the
#   tracker sees content differences, not prompt overhead.
#
# REQUIRES: /data/sae_checkpoints/qwen_3b_instruct/ exists (from triviaqa run)
#
# Submit: kubectl apply -f jobs/k8s/mech-qwen3b-s0-halueval.yaml
# Logs:   kubectl logs -f -l job-name=ghosttrack-qwen3b-s0-halueval -n gp-engine-mizzou-dcps
# Delete: kubectl delete job ghosttrack-qwen3b-s0-halueval -n gp-engine-mizzou-dcps

apiVersion: batch/v1
kind: Job
metadata:
  name: ghosttrack-qwen3b-s0-halueval
  namespace: gp-engine-mizzou-dcps
  labels:
    app: ghosttrack-qwen3b-s0-halueval
    project: ghosttrack
    phase: trackdetect
    model: qwen-3b-instruct
    seed: "0"
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: ghosttrack-qwen3b-s0-halueval
        project: ghosttrack
    spec:
      restartPolicy: Never
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: nvidia.com/gpu.product
                operator: In
                values:
                - NVIDIA-A100-SXM4-80GB
                - NVIDIA-A100-80GB-PCIe
                - NVIDIA-A100-PCIE-40GB
                - NVIDIA-A40
                - NVIDIA-L40
                - NVIDIA-L40S
                - NVIDIA-RTX-A6000
                - NVIDIA-GH200-480GB
                - NVIDIA-H200-NVL
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            preference:
              matchExpressions:
              - key: nvidia.com/gpu.product
                operator: In
                values:
                - NVIDIA-A100-SXM4-80GB
                - NVIDIA-A100-80GB-PCIe
                - NVIDIA-GH200-480GB
                - NVIDIA-H200-NVL
          - weight: 60
            preference:
              matchExpressions:
              - key: nvidia.com/gpu.product
                operator: In
                values:
                - NVIDIA-A100-PCIE-40GB
                - NVIDIA-A40
                - NVIDIA-L40
                - NVIDIA-L40S
                - NVIDIA-RTX-A6000
      tolerations:
      - key: "nvidia.com/gpu"
        operator: "Exists"
        effect: "NoSchedule"
      containers:
      - name: ghosttrack
        image: khurramkhalil/ghosttrack:latest
        imagePullPolicy: Always
        workingDir: /workspace/GhostTrack
        command: ["/bin/bash", "-c"]
        args:
          - |
            set -e
            LOG=/data/logs/qwen3b_halueval_$(date +%s).log
            mkdir -p /data/logs
            exec > >(tee "$LOG") 2>&1
            echo "=== GhostTrack: HaluEval Track+Detect | Qwen2.5-3B-Instruct | Seed 0 ==="
            python3 -c '
            import urllib.request
            base = "https://raw.githubusercontent.com/khurramkhalil/GhostTrack/main/"
            files = [
              ("ghosttrack/tracking/extractor.py",       "/workspace/GhostTrack/ghosttrack/tracking/extractor.py"),
              ("ghosttrack/data/__init__.py",             "/workspace/GhostTrack/ghosttrack/data/__init__.py"),
              ("ghosttrack/detection/detector.py",       "/workspace/GhostTrack/ghosttrack/detection/detector.py"),
              ("scripts/run_mechanism_study.py",         "/workspace/GhostTrack/scripts/run_mechanism_study.py"),
              ("config/model_configs/qwen-3b-instruct.yaml", "/workspace/GhostTrack/config/model_configs/qwen-3b-instruct.yaml"),
            ]
            [(urllib.request.urlretrieve(base+s, d), print("OK", s)) for s, d in files]
            '
            nvidia-smi

            MODEL="Qwen/Qwen2.5-3B-Instruct"
            CONFIG="config/model_configs/qwen-3b-instruct.yaml"
            OUT_DIR="/data/experiments/phase2_mechanism/qwen_3b_instruct_s0_halueval"
            SAE_DIR="/data/sae_checkpoints/qwen_3b_instruct"

            echo "=== Step 1: Load HaluEval dataset ==="
            python scripts/run_mechanism_study.py \
              --model "$MODEL" \
              --config "$CONFIG" \
              --phase data \
              --dataset halueval \
              --halueval-split qa \
              --output-dir "$OUT_DIR" \
              --data-dir /data/datasets \
              --seed 0 \
              --device cuda

            echo "=== Step 2: Run hypothesis tracker ==="
            python scripts/run_mechanism_study.py \
              --model "$MODEL" \
              --config "$CONFIG" \
              --phase track \
              --output-dir "$OUT_DIR" \
              --data-dir /data/datasets \
              --sae-checkpoint-dir "$SAE_DIR" \
              --seed 0 \
              --device cuda

            echo "=== Step 3: Fit detector ==="
            python scripts/run_mechanism_study.py \
              --model "$MODEL" \
              --config "$CONFIG" \
              --phase detect \
              --output-dir "$OUT_DIR" \
              --seed 0

            echo "=== Done ==="
        resources:
          requests:
            nvidia.com/gpu: 1
            memory: "32Gi"
            cpu: "2"
          limits:
            nvidia.com/gpu: 1
            memory: "32Gi"
            cpu: "2"
        envFrom:
        - secretRef:
            name: ghosttrack-secrets
        - configMapRef:
            name: ghosttrack-config
        volumeMounts:
        - mountPath: /data
          name: ghosttrack-data
        - mountPath: /dev/shm
          name: dshm
      volumes:
      - name: ghosttrack-data
        persistentVolumeClaim:
          claimName: ghosttrack-data
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 2Gi
