# GhostTrack — Llama-3.2-3B-Instruct | Seed 0 | Full Pipeline
#
# Phases: SAE training (Wikipedia) → TriviaQA generation (instruct prompts)
#         → LLM judge → track → detect
#
# Why this model: Llama-3.2-3B-Instruct is Meta's standard small model,
# widely cited in the community. Provides cross-lab generalization alongside
# Qwen and Phi. Expected TriviaQA accuracy: ~40-55%.
#
# Architecture: 28 layers, d_model=3072, d_hidden=12288 (4x SAE)
# GPU memory:
#   SAE training: model (~6 GB) + one SAE layer (~576 MB) + activations ≈ 12 GB
#   Tracking:     model (~6 GB) + all 28 SAE checkpoints (~8 GB) ≈ 16 GB
#   → requires ≥ 40 GB GPU
#
# IMPORTANT: Llama-3.2-3B-Instruct is a GATED model on HuggingFace.
# You must:
#   1. Accept the Meta license at https://huggingface.co/meta-llama/Llama-3.2-3B-Instruct
#   2. Ensure HF_TOKEN is set in ghosttrack-secrets (key: HUGGING_FACE_HUB_TOKEN)
#
# Timing estimate: SAE ~6h, TriviaQA gen ~40min, judge ~30min, track ~3.5h
#
# Submit: kubectl apply -f jobs/k8s/mech-llama3b-s0.yaml
# Logs:   kubectl logs -f -l job-name=ghosttrack-llama3b-s0 -n gp-engine-mizzou-dcps
# Delete: kubectl delete job ghosttrack-llama3b-s0 -n gp-engine-mizzou-dcps

apiVersion: batch/v1
kind: Job
metadata:
  name: ghosttrack-llama3b-s0
  namespace: gp-engine-mizzou-dcps
  labels:
    app: ghosttrack-llama3b-s0
    project: ghosttrack
    phase: full-pipeline
    model: llama-3.2-3b-instruct
    seed: "0"
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: ghosttrack-llama3b-s0
        project: ghosttrack
    spec:
      restartPolicy: Never
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: nvidia.com/gpu.product
                operator: In
                values:
                - NVIDIA-A100-SXM4-80GB
                - NVIDIA-A100-80GB-PCIe
                - NVIDIA-A100-PCIE-40GB
                - NVIDIA-A40
                - NVIDIA-L40
                - NVIDIA-L40S
                - NVIDIA-RTX-A6000
                - NVIDIA-GH200-480GB
                - NVIDIA-H200-NVL
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            preference:
              matchExpressions:
              - key: nvidia.com/gpu.product
                operator: In
                values:
                - NVIDIA-A100-SXM4-80GB
                - NVIDIA-A100-80GB-PCIe
                - NVIDIA-GH200-480GB
                - NVIDIA-H200-NVL
          - weight: 60
            preference:
              matchExpressions:
              - key: nvidia.com/gpu.product
                operator: In
                values:
                - NVIDIA-A100-PCIE-40GB
                - NVIDIA-A40
                - NVIDIA-L40
                - NVIDIA-L40S
                - NVIDIA-RTX-A6000
      tolerations:
      - key: "nvidia.com/gpu"
        operator: "Exists"
        effect: "NoSchedule"
      containers:
      - name: ghosttrack
        image: khurramkhalil/ghosttrack:latest
        imagePullPolicy: Always
        workingDir: /workspace/GhostTrack
        command: ["/bin/bash", "-c"]
        args:
          - |
            set -e
            echo "=== GhostTrack: Full Pipeline | Llama-3.2-3B-Instruct | Seed 0 ==="
            pip install openai -q

            python3 -c '
            import urllib.request
            base = "https://raw.githubusercontent.com/khurramkhalil/GhostTrack/main/"
            files = [
              ("ghosttrack/tracking/extractor.py",       "/workspace/GhostTrack/ghosttrack/tracking/extractor.py"),
              ("ghosttrack/data/__init__.py",             "/workspace/GhostTrack/ghosttrack/data/__init__.py"),
              ("ghosttrack/data/triviaqa_generator.py",  "/workspace/GhostTrack/ghosttrack/data/triviaqa_generator.py"),
              ("ghosttrack/detection/detector.py",       "/workspace/GhostTrack/ghosttrack/detection/detector.py"),
              ("scripts/run_mechanism_study.py",         "/workspace/GhostTrack/scripts/run_mechanism_study.py"),
              ("scripts/score_triviaqa.py",              "/workspace/GhostTrack/scripts/score_triviaqa.py"),
              ("config/model_configs/llama-3.2-3b-instruct.yaml", "/workspace/GhostTrack/config/model_configs/llama-3.2-3b-instruct.yaml"),
            ]
            [(urllib.request.urlretrieve(base+s, d), print("OK", s)) for s, d in files]
            '
            nvidia-smi

            MODEL="meta-llama/Llama-3.2-3B-Instruct"
            CONFIG="config/model_configs/llama-3.2-3b-instruct.yaml"
            CACHE_PATH="/data/datasets/triviaqa_meta-llama-Llama-3.2-3B-Instruct.json"
            OUT_DIR="/data/experiments/phase2_mechanism/llama_3b_instruct_s0_triviaqa"
            SAE_DIR="/data/sae_checkpoints/llama_3b_instruct"

            echo "=== Step 1: Train SAEs on Wikipedia ==="
            python scripts/run_mechanism_study.py \
              --model "$MODEL" \
              --config "$CONFIG" \
              --phase sae \
              --output-dir "$OUT_DIR" \
              --data-dir /data/datasets \
              --sae-checkpoint-dir "$SAE_DIR" \
              --n-tokens 5000000 \
              --batch-size-extract 4 \
              --batch-size-sae 256 \
              --seed 0 \
              --device cuda

            echo "=== Step 2: Generate TriviaQA completions (instruct mode) ==="
            python scripts/run_mechanism_study.py \
              --model "$MODEL" \
              --config "$CONFIG" \
              --phase data \
              --dataset triviaqa \
              --output-dir "$OUT_DIR" \
              --data-dir /data/datasets \
              --seed 0 \
              --device cuda

            echo "=== Step 3: LLM-judge TriviaQA completions ==="
            python scripts/score_triviaqa.py \
              --triviaqa-cache "$CACHE_PATH" \
              --output-dir "$OUT_DIR" \
              --api-key E0fTkLAw3ZqcsYuugDKbwh5VcTMmto8x \
              --batch-size 10 \
              --max-workers 10

            echo "=== Step 4: Run hypothesis tracker ==="
            python scripts/run_mechanism_study.py \
              --model "$MODEL" \
              --config "$CONFIG" \
              --phase track \
              --output-dir "$OUT_DIR" \
              --data-dir /data/datasets \
              --sae-checkpoint-dir "$SAE_DIR" \
              --seed 0 \
              --device cuda

            echo "=== Step 5: Fit detector ==="
            python scripts/run_mechanism_study.py \
              --model "$MODEL" \
              --config "$CONFIG" \
              --phase detect \
              --output-dir "$OUT_DIR" \
              --seed 0

            echo "=== Done ==="
        resources:
          requests:
            nvidia.com/gpu: 1
            memory: "48Gi"
            cpu: "4"
          limits:
            nvidia.com/gpu: 1
            memory: "48Gi"
            cpu: "4"
        envFrom:
        - secretRef:
            name: ghosttrack-secrets
        - configMapRef:
            name: ghosttrack-config
        volumeMounts:
        - mountPath: /data
          name: ghosttrack-data
        - mountPath: /dev/shm
          name: dshm
      volumes:
      - name: ghosttrack-data
        persistentVolumeClaim:
          claimName: ghosttrack-data
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 4Gi
