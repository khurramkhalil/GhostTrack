┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                    GhostTrack - Quick Reference                     ┃
┃                   GPT-2 Small & Medium Pipelines                    ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

┌─────────────────────────────────────────────────────────────────────┐
│ 🚀 QUICK LAUNCH                                                     │
└─────────────────────────────────────────────────────────────────────┘

  GPT-2 Small (12 layers, ~15 hours):
    ./launch_model_analysis.sh gpt2-small

  GPT-2 Medium (24 layers, ~60 hours):
    ./launch_model_analysis.sh gpt2-medium

  Both models:
    ./launch_model_analysis.sh both

┌─────────────────────────────────────────────────────────────────────┐
│ 📊 MODEL COMPARISON                                                 │
└─────────────────────────────────────────────────────────────────────┘

  ┌──────────────────┬──────────────┬──────────────┐
  │ Specification    │ Small        │ Medium       │
  ├──────────────────┼──────────────┼──────────────┤
  │ Model ID         │ gpt2         │ gpt2-medium  │
  │ Layers           │ 12           │ 24           │
  │ Hidden Dim       │ 768          │ 1024         │
  │ Parameters       │ 117M         │ 345M         │
  │ SAE Hidden       │ 4096         │ 5120         │
  │ Extract Batch    │ 32           │ 16 ⚡        │
  │ Train Batch      │ 256          │ 128 ⚡       │
  │ Est. Time        │ ~15 hours    │ ~60 hours    │
  │ Storage          │ ~25 GB       │ ~70 GB       │
  └──────────────────┴──────────────┴──────────────┘

  ⚡ = Optimized to avoid OOM while maintaining good GPU utilization

┌─────────────────────────────────────────────────────────────────────┐
│ 📁 DIRECTORY STRUCTURE                                              │
└─────────────────────────────────────────────────────────────────────┘

  config/model_configs/
    ├── gpt2-small.yaml       # Configuration for GPT-2 Small
    └── gpt2-medium.yaml      # Configuration for GPT-2 Medium

  data/cache/
    ├── gpt2-small/
    │   └── hidden_states/    # Extracted activations (Small)
    └── gpt2-medium/
        └── hidden_states/    # Extracted activations (Medium)

  models/checkpoints/
    ├── gpt2-small/           # Trained SAEs (Small)
    └── gpt2-medium/          # Trained SAEs (Medium)

  results/
    ├── gpt2-small/           # Analysis results (Small)
    └── gpt2-medium/          # Analysis results (Medium)

┌─────────────────────────────────────────────────────────────────────┐
│ 🔍 MONITORING                                                       │
└─────────────────────────────────────────────────────────────────────┘

  Check job status:
    squeue -u $USER

  Watch output (Small):
    tail -f gpt2s-full_*.out

  Watch output (Medium):
    tail -f gpt2m-full_*.out

  Check GPU usage:
    ssh <compute-node>
    nvidia-smi -l 1

  Monitor disk usage:
    watch -n 60 'du -sh data/cache/gpt2-*/ models/checkpoints/gpt2-*/'

┌─────────────────────────────────────────────────────────────────────┐
│ 🔧 TUNING BATCH SIZES                                               │
└─────────────────────────────────────────────────────────────────────┘

  If OOM (Out of Memory):
    Edit the .sbatch file variables:

    Small:
      EXTRACT_BATCH_SIZE=24    # Down from 32
      TRAIN_BATCH_SIZE=192     # Down from 256

    Medium:
      EXTRACT_BATCH_SIZE=12    # Down from 16
      TRAIN_BATCH_SIZE=96      # Down from 128

  If Under-utilized (low GPU usage):
    Increase batch sizes in opposite direction

┌─────────────────────────────────────────────────────────────────────┐
│ 📈 PIPELINE PHASES (GPT-2 Medium)                                   │
└─────────────────────────────────────────────────────────────────────┘

  Phase 1: Hidden State Extraction   (~60 min)
    ✓ 4 GPUs parallel, 6 layers each
    ✓ Batch size: 16
    ✓ Output: 24 × 10M tokens

  Phase 2: SAE Training              (~90 min per layer)
    ✓ 4 GPUs parallel
    ✓ Batch size: 128
    ✓ 20 epochs each
    ✓ Output: 24 trained SAEs

  Phase 3: Hypothesis Tracking       (~30 min)
    ✓ Track features across generation
    ✓ Semantic clustering

  Phase 4: Hallucination Detection   (~20 min)
    ✓ Entropy & churn analysis
    ✓ Classification scores

  Phase 5: Evaluation & Metrics      (~10 min)
    ✓ AUROC, Precision, Recall, F1
    ✓ Final report generation

┌─────────────────────────────────────────────────────────────────────┐
│ 📚 DOCUMENTATION                                                    │
└─────────────────────────────────────────────────────────────────────┘

  Overview of changes:
    REORGANIZATION_SUMMARY.md

  Model configuration details:
    docs/MODEL_CONFIGS.md

  GPT-2 Medium pipeline guide:
    docs/GPT2_MEDIUM_PIPELINE.md

  General usage:
    README.md
    docs/USAGE.md

┌─────────────────────────────────────────────────────────────────────┐
│ ⚠️  IMPORTANT NOTES                                                 │
└─────────────────────────────────────────────────────────────────────┘

  • Batch sizes are optimized for A100 40GB GPUs
  • Medium model uses HALF the batch sizes of Small
  • All 5 phases are integrated in the Medium pipeline
  • Results are automatically organized by model
  • Monitor first hour to ensure no OOM issues

┌─────────────────────────────────────────────────────────────────────┐
│ 📊 EXPECTED PERFORMANCE                                             │
└─────────────────────────────────────────────────────────────────────┘

  GPU Utilization:
    ✓ Extraction: 75-85%
    ✓ Training:   80-90%

  Quality Metrics (Target):
    ✓ Reconstruction Loss: < 0.01
    ✓ Sparsity: 50-100 active features
    ✓ AUROC: > 0.90
    ✓ F1 Score: > 0.85

┌─────────────────────────────────────────────────────────────────────┐
│ 🎯 NEXT STEPS                                                       │
└─────────────────────────────────────────────────────────────────────┘

  1. Launch GPT-2 Medium pipeline:
     ./launch_model_analysis.sh gpt2-medium

  2. Monitor progress (first hour critical)

  3. After completion, compare with Small:
     python analysis/compare_models.py

  4. Generate visualizations:
     python visualization/dashboard.py --results results/gpt2-medium

  5. Export results for paper:
     python scripts/export_paper_results.py --model gpt2-medium

┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
